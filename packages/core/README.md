# @gehu-js/core

Platform-agnostic Gehu store engine.

```sh
bun add @gehu-js/core
```

Exports `createStore`, `buildStore`, `linkedStore`, `memoryStorage`, and the shared signal/runtime types used by the adapter packages.

Full docs: https://github.com/knoxpo/gehu#readme
