# @gehu-js/react

React bindings for Gehu stores.

```sh
bun add @gehu-js/react @gehu-js/core
```

Exports `useStore`, `useSignal`, `useResource`, `useMutation`, `GehuProvider`, and the server-safe `@gehu-js/react/server` entry.

Full docs: https://github.com/knoxpo/gehu#readme
