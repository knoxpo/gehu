# @gehu-js/angular

Angular adapter for Gehu stores.

```sh
bun add @gehu-js/angular @gehu-js/core
```

Exports `provideGehu`, `provideStore`, `injectStore`, SSR hydration helpers, and the `ngrx-compat` subpath.

Full docs: https://github.com/knoxpo/gehu#readme
