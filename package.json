{
  "name": "gehu",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "bun run --filter '@gehu/*' build",
    "typecheck": "bun run --filter '@gehu/*' typecheck",
    "test": "bun test"
  },
  "catalogs": {
    "base": {
      "typescript": ">=5.8.0 <7.0.0"
    }
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "catalog:base"
  }
}
